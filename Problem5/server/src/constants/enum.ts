export enum ModelOfProduct {
  XL = 'XL',
  M = 'M',
  L = 'L'
}

export enum RoleOfUser {
  ADMIN = 'ADMIN',
  USER = 'USER'
}

export enum Gender {
  Male = 'Male',
  Female = 'Female',
  Other = 'Other'
}

export enum StatusActivity {
  Unverify = 'Unverify',
  Verified = 'Verified',
  Banned = 'Banned'
}

export enum TokenType {
  RefreshToken = 'RefreshToken',
  AccessToken = 'AccessToken',
  PublicKey = 'PublicKey',
  EmailVerifyToken = 'EmailVerifyToken',
  ForgotPasswordToken = 'ForgotPasswordToken'
}
